.title #{t :users}
%table.table.table-striped.table-bordered
  %thead
    %tr
      %th
        Email 
      %th 
        User Role  
      %th
        Actions
  %tbody
    - @users.each do |user|
      %tr 
        %td
          #{user.email}
        %td
          - if user.admin
            Admin
          - else   
            Other User
        %td
          = link_to "Show", user_url(user), class: "btn btn-primary"